{
    // Due to the fact that Blazor WebAssembly client is deployed as a static web page, 
    // the environment selection is done in following way.
    // Upon deploy (add custom build step in CI/CD pipeline):
    // - rename appservice.<ENV>.json file to 'appservice.json'
    // - remove remaining appservice.<ENV>.josn files
    "PingPongApi": "https://localhost:5001/"
}